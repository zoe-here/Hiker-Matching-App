services:
  db:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_DB: matchme
      POSTGRES_USER: matchme
      POSTGRES_PASSWORD: matchme
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: